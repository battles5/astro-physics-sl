# ============================================================================
# .Rprofile — pulsar-sl project
# Activates renv for reproducible package management
# ============================================================================

source("renv/activate.R")

# Set CRAN mirror
options(repos = c(CRAN = "https://cloud.r-project.org"))

# Print project info on startup
if (interactive()) {
  message("\n=== pulsar-sl ===")
  message("Superconductivity (regression) + Pulsar Stars (classification)")
  message("Run source('R/00_setup.R') to load all libraries and config.\n")
}
